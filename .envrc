watch_file .prototools
watch_dir .moon/proto

if ! command -v proto >/dev/null 2>&1; then
  log_error "proto is required for this repository: https://moonrepo.dev/proto"
  exit 1
fi

proto install --quiet
eval "$(proto activate bash --export --no-init)"
