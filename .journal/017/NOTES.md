---
id: 017
title: ctrlkit foundation
started: 2026-05-25
---

## 2026-05-25 13:19 — Kickoff
Goal for the session: implement the standalone `internal/ctrlkit` foundation with subpackages for controller naming, metadata/ownership, conditions, readiness, and generic artifact ConfigMap validation, without modifying existing controllers or other repo surfaces.
Current state of the world: `master` is at `de42f99` with `CardanoNetwork` and `CardanoDBSync` controllers already implemented; prior closed sessions through `015` are complete, and `.journal/016` already exists as an active tracked session. The new work should stay scoped to `internal/ctrlkit/**`.
Plan: prime this session on `journal/jmgilman`, create an isolated Worktrunk branch for implementation, start the repo dev stack, add the ctrlkit packages and focused tests, then run `moon run root:test`, `moon run root:check`, and `git diff --check`.

## 2026-05-25 13:21 — Dev stack ready
Created implementation worktree `/Users/josh/code/meigma/yacd/.wt/feat-ctrlkit-foundation` on branch `feat/ctrlkit-foundation`. Ran `direnv allow` and `moon run root:dev-up`; the Kind/Tilt dev stack reported `YACD dev stack is ready` with Tilt UI on `http://localhost:10350/`.

## 2026-05-25 13:28 — ctrlkit foundation pushed
Implemented only `internal/ctrlkit/**`: `names`, `metadata`, `conditions`, `readiness`, and `artifacts`, each with package docs and focused table-driven tests. Validation passed with `go test ./internal/ctrlkit/...`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `abb9747` (`feat(ctrlkit): add controller utility foundation`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 14:29 — ctrlkit controller integration pushed
Integrated `internal/ctrlkit` into the `CardanoNetwork` and `CardanoDBSync` controllers as a refactor-only slice. Added shared `ctrlkit/apply` and `ctrlkit/storage` helpers, extended artifact/name contracts, removed duplicated db-sync network artifact helpers, and rewired controller naming, metadata merge, ownership, conditions, readiness predicates, artifact validation, and requested storage-class handling through ctrlkit while preserving controller-specific wrappers and messages. Validation passed with `go test ./internal/ctrlkit/...`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `6482867` (`refactor(ctrlkit): share controller helper logic`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 15:08 — review feedback cleanup pushed
Addressed review feedback that `ctrlkit` should not own CardanoNetwork domain contracts. Added `internal/cardano/networkartifacts` for the operator-side CardanoNetwork artifact schema/key contract, removed the unused `ctrlkit/artifacts` `Result` validator and string-based reason classification, and renamed `metadata.MergeStringMap` to `OverlayStringMap` to make preserve-existing-key semantics explicit. Validation passed with `go test ./internal/ctrlkit/... ./internal/cardano/networkartifacts`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `c62c213` (`refactor(ctrlkit): move network artifact contract`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 17:25 — controller contract cleanup pushed
Addressed the second architecture review pass. Moved producer and consumer artifact validation workflows into `internal/cardano/networkartifacts`, routed `CardanoNetwork` sidecar deployment gates through a local helper backed by `ctrlkit/readiness.DeploymentAvailable`, made `ctrlkit/names.DNSLabelWithSuffix` sanitize and hash unsafe or truncated suffixes, and centralized requested storage-class drift comparison in `ctrlkit/storage`. Validation passed with `go test ./internal/ctrlkit/... ./internal/cardano/networkartifacts`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `3a69672` (`refactor(ctrlkit): centralize controller contract checks`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 17:52 — artifact boundary split pushed
Addressed follow-up review feedback on package boundaries. Kept `internal/cardano/networkartifacts` Kubernetes-free by limiting it to the CardanoNetwork artifact schema and data-key contract, moved CR status and `corev1.ConfigMap` producer/consumer validation into `internal/controller/networkartifacts`, and removed `SchemaVersion` from `ctrlkit/artifacts.Contract` so the generic validator remains an explicit data-only helper. Validation passed with `go test ./internal/cardano/networkartifacts ./internal/controller/networkartifacts ./internal/ctrlkit/artifacts`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `fed7412` (`refactor(ctrlkit): split artifact validation boundary`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 18:27 — owned apply cleanup pushed
Addressed the latest architecture review pass. Added `ctrlkit/apply.ApplyOwnedObject` for the repeated owned-child get/create, owner validation, optional drift validation, mutation, equality check, and patch/update skeleton; adopted it for both controllers' PVC, Deployment, and Service apply paths while preserving controller-specific conflict mapping and immutable drift checks. Moved the requested storage-class annotation key into `internal/controller/annotations` so `ctrlkit/storage` stays generic over caller-provided annotation keys, and removed the unused owner-conflict reason taxonomy from `ctrlkit/metadata`. Validation passed with `moon run root:test`, `moon run root:check`, and `git diff --check`; a plain controller `go test` probe still failed due missing envtest assets outside the Moon task environment. Committed `edcd464` (`refactor(ctrlkit): share owned resource apply`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 18:31 — annotation boundary cleanup pushed
Removed the remaining YACD annotation constants from `ctrlkit/artifacts` so the package is generic over caller-owned metadata keys. Moved artifact schema/hash annotation constants into `internal/controller/annotations` and made `ctrlkit/artifacts.HasPublishedData` accept caller-provided annotation keys. Validation passed with `go test ./internal/ctrlkit/... ./internal/controller/networkartifacts`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `eba4e54` (`refactor(ctrlkit): keep artifact annotations in controllers`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 18:55 — simple owned-child apply cleanup pushed
Addressed the next review pass. Strengthened the `ctrlkit/apply` package contract documentation around desired object ownership, defaulting, immutable drift validation, mutation ownership, and status-facing conflict mapping. Moved the localnet fingerprint annotation key into `internal/controller/annotations` and made `internal/controller/networkartifacts` consume it. Adopted `ctrlkit/apply.ApplyOwnedObject` for the straightforward CardanoNetwork artifact publisher ServiceAccount/Role/RoleBinding paths and CardanoDBSync ConfigMap/pgpass Secret paths, leaving special artifact recovery, faucet token preservation, delete, and suspend flows local. Validation passed with `go test ./internal/ctrlkit/... ./internal/cardano/networkartifacts ./internal/controller/networkartifacts`, `moon run root:test`, `moon run root:check`, and `git diff --check`; a plain controller `go test` probe still failed due missing envtest assets outside Moon. Committed `975df77` (`refactor(ctrlkit): apply simple owned children`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 19:11 — apply lifecycle and schema cleanup pushed
Addressed the latest review pass. Made consumer-side network artifact validation reject unsupported schema versions before applying the current data-key contract, documented and tested the `ctrlkit/apply.ApplyOwnedObject` callback lifecycle so create uses the defaulted desired object directly while Validate/Mutate run only for existing objects, locked zero-value `UpdateMode` to merge patch, and renamed the stale `applyDBSyncMetricsService` helper to `applyDBSyncService`. Validation passed with `go test ./internal/ctrlkit/... ./internal/cardano/networkartifacts ./internal/controller/networkartifacts`, `moon run root:test`, `moon run root:check`, and `git diff --check`. Committed `d7effdd` (`refactor(ctrlkit): clarify apply lifecycle`) and pushed `feat/ctrlkit-foundation` to origin.

## 2026-05-25 20:48 — PR opened and CI verified
Found the implementation worktree had one local unpushed commit plus remaining in-scope controller/ctrlkit cleanup. Validated the full local state with `moon run root:test`, `moon run root:check`, and `git diff --check`, committed `4f994ab` (`refactor(ctrlkit): finish controller foundation cleanup`), and pushed `feat/ctrlkit-foundation`. Opened PR #33 (`refactor(ctrlkit): share controller foundations`) at https://github.com/meigma/yacd/pull/33. GitHub checks completed successfully for head `4f994ab`: `ci` passed, `Kusari Inspector` passed, and release/image dry-run jobs were skipped as expected.
