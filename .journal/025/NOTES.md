---
id: 025
title: Session 025
started: 2026-05-26
---

## 2026-05-26 21:00 — Kickoff
Goal for the session: start a new journal-backed work session; wait for the user's actual implementation or exploration request before making product changes.
Current state of the world: `master` is clean at `f5bbfbb` from PR #42, which fixed the dev stack by adding the `--default-cardano-testnet-image` override, chart value, and Tilt-local cardano-testnet rebuild path. The journal branch `journal/jmgilman` is clean and up to date, with sessions 022-024 read for context. Session 024 completed the post-refactor manual functional pass and left the dev stack stopped. The immediately preceding discussion explored the public-network db-sync topology problem: the current dedicated follower-node shape is clean for ownership but wasteful for preview/preprod/mainnet, and the leading architectural option was to keep `CardanoDBSync` as the service/config/status owner while letting `CardanoNetwork` be the sole primary Pod composition authority for any db-sync sidecar placement.
Plan: wait for the user's next request, then choose or create an implementation worktree, start `moon run root:dev-up` from that worktree before substantive implementation unless waived, and keep session notes updated at meaningful checkpoints.

## 2026-05-26 21:17 — Current master db-sync placement assessment
Reviewed current `master` (`f5bbfbb`) after the recent refactor stack for the public-network db-sync placement problem. Current code still has two distinct ownership domains: `CardanoNetwork` owns the primary Deployment and optional socket sidecars (Ogmios/Kupo/Faucet), while `CardanoDBSync` owns a separate two-container Deployment with a dedicated follower node plus db-sync, its state PVCs, pgpass/config, metrics Service, managed Postgres, and status. `CardanoDBSync` has no placement mode today; `spec.followerNode` only customizes the dedicated follower path.

Assessment: keep `CardanoDBSync` as the service/config/database/status owner and make `CardanoNetwork` the sole primary-Pod composition authority for any db-sync sidecar attachment. Do not try to make both controllers own the same Deployment. Add an explicit `spec.placement.mode` to `CardanoDBSync`, defaulting to the existing dedicated-follower behavior. In primary-sidecar mode, `CardanoDBSync` should reconcile DB/config/pgpass/state/metrics objects, while `CardanoNetwork` lists eligible claims and appends the db-sync init/container/volumes to the primary Deployment only when there is exactly one valid claim.

Key risks: current `CardanoNetwork` still rejects public mode, so public-network value needs a separate public-node/artifact builder slice; db-sync metrics defaults to port `8080`, which collides with the faucet in a shared Pod; primary-sidecar failure couples to the primary Pod's readiness and normal Service endpoints; config/Secret/PVC hash changes must roll the primary Deployment; sidecar dependency failures should detach or withhold attachment rather than wedge the primary node; multiple primary-sidecar claimants should produce `PlacementConflict` and attach none.

## 2026-05-26 21:31 — Implementation start
User approved the placement API + conflict detection slice. Created Worktrunk branch/worktree `feat/cardanodbsync-placement` at `/Users/josh/code/meigma/yacd/.wt/feat-cardanodbsync-placement` from `master` (`f5bbfbb`). Ran `moon run root:dev-up` from the implementation worktree; it created `kind-yacd-dev`, started Tilt in the background, and exited successfully with the controller ready. Proceeding with API/controller/test implementation there; no product edits will be made in the primary checkout or journal worktree.

## 2026-05-26 22:28 — Placement API slice implemented
Implemented and committed `feat(cardanodbsync): add placement conflict detection` on `feat/cardanodbsync-placement` as `a6fc1d6`. The slice adds `CardanoDBSync.spec.placement.mode` with `dedicatedFollower` and `primarySidecar`, CRD validation for invalid modes and `primarySidecar` plus `followerNode`, early reconcile placement gating before database resolution, same-namespace primary-sidecar conflict detection, and a same-kind peer watch so conflicts clear when claimants are created, deleted, or moved between networks. Primary-sidecar mode intentionally stops at status reporting and suspension of any owned follower Deployment; it does not inject db-sync into the CardanoNetwork primary Pod.

Verification: `moon run root:generate`, `moon run root:test`, `moon run root:check`, and `git diff --check` passed from `/Users/josh/code/meigma/yacd/.wt/feat-cardanodbsync-placement`. `root:check` first exposed that its generated-artifact check fails while generated API/CRD edits are unstaged; after staging the intended generated files, the check passed. The dev stack remains running from the implementation worktree for follow-up review.

## 2026-05-27 10:27 — Status-based primary-sidecar contract
Implemented the status-contract refactor on top of the primary-sidecar runtime work. `CardanoDBSync.status.placement` now reports the effective mode and publishes an attachable `primarySidecar` contract only when db-sync config, pgpass, state PVC, and metrics Service are current. Added `SidecarMaterialReady` and an opaque `sha256:` revision over sidecar-mounted material. Non-attachable paths, including dedicated follower, conflicts, unsupported specs, dependency waits, and apply-blocked states, clear the `primarySidecar` contract.

`CardanoNetwork` now composes the primary Pod from `CardanoDBSync.status` only: it lists primary-sidecar claims, requires exactly one fresh `SidecarMaterialReady=True` claim with resource names and revision, keeps local-network/port validation, and renders one rollout annotation `yacd.meigma.io/dbsync-sidecar-revision`. Removed the direct DB Sync ConfigMap/Secret/PVC reads and watches from the network controller; the remaining DB Sync watch requeues on spec/status changes for attachment and removal.

Verification: `moon run root:generate`, `moon run root:test`, and `git diff --check` passed. A plain `moon run root:check` ran lint/tool tests and then stopped at its `git diff --exit-code -- api charts/yacd/crds` generated-artifact gate because this API/CRD change is intentionally unstaged; rerunning `root:check` with a temporary git index seeded with the current generated API/CRD files passed through generated-artifact, Helm, and Chainsaw checks without touching the real index.

## 2026-05-27 11:22 — Primary-sidecar review fixes
Implemented the narrow review-fix slice for primary-sidecar attachment. `CardanoNetwork` now publishes `DBSyncAttachmentReady`, leaves it out of aggregate `Ready` when no primary-sidecar attachment is rendered, and puts it before `NodeReady` when db-sync is attached. The db-sync attachment readiness path checks primary Deployment presence/staleness plus live Pod status for the named `cardano-db-sync` container, so a db-sync crashloop is attributed to the attachment condition while a healthy db-sync sidecar with an unhealthy node leaves `NodeReady` as the blocker. `DBSyncAttachmentPending` now uses the normal primary-workload readiness requeue interval.

Also tightened primary-sidecar db-sync metrics selection to the primary network selector labels plus the db-sync discriminator, leaving dedicated-follower metrics selection unchanged. Updated `DESIGN.md` to clarify that dedicated follower remains the default/no-primary-restart path and `primarySidecar` is the explicit socket-sharing exception that rolls the primary Deployment.

Verification: `moon run root:generate`, `moon run root:test --summary minimal`, `git diff --check`, and `moon run root:check --summary minimal` with the temporary-index workaround passed. Plain `root:check --summary minimal` still fails at the generated-artifact diff gate because this branch's generated API/CRD edits remain intentionally unstaged in the real index.

## 2026-05-27 12:12 — Placement transition hardening
Addressed the follow-up review concerns around placement flips, rollout churn, and requested-but-unattached network status. `CardanoDBSync` now uses a two-phase placement handoff: primary-sidecar mode scales any owned dedicated Deployment to zero and waits for dedicated db-sync Pods to terminate before publishing attachable material, while dedicated-follower mode waits for the primary Deployment and live primary Pods to drop the db-sync sidecar before creating the dedicated Deployment. Transition waits publish `PlacementTransitionPending` and clear `SidecarMaterialReady`/primary-sidecar status.

Changed db-sync credential rollout input from source Secret `ResourceVersion` to a SHA-256 fingerprint of the rendered pgpass material, so unrelated external Secret metadata churn no longer changes the primary-sidecar revision or rolls the primary node. `CardanoNetwork` now reports requested-but-unattached claims as `DBSyncAttachmentReady=False` with `DBSyncAttachmentPending`, `PlacementConflict`, or `UnsupportedSpec` instead of misreporting them as `DBSyncAttachmentNotRequested`; non-attached cases still do not hold aggregate `Ready` back.

Verification: `moon run root:generate`, `moon run root:test --summary minimal`, `moon run root:check --summary minimal` with the temporary-index workaround, and `git diff --check` passed. A plain `root:check --summary minimal` still fails only at the generated-artifact diff gate because generated API/CRD edits are intentionally unstaged in the real index.

## 2026-05-27 12:51 — Primary-sidecar quality cleanup
Addressed the quality-pass review without changing the placement API or controller ownership split. Added `internal/cardano/primarypod` as the shared primary Pod vocabulary for workload names, selector labels, default ports, port names, and port ownership so `CardanoDBSync` no longer carries a private copy of CardanoNetwork's primary selector/port constants. `CardanoDBSync` still owns the db-sync sidecar attachment contract and status; `CardanoNetwork` still consumes that contract and remains the only controller mutating the primary Deployment.

Cleaned up the attachment resource naming (`StatePVCName`), removed unused pre-staged db-sync detail annotations from CardanoNetwork's owned annotation set, moved `planSpec` to a package-level planner helper, and made primary-sidecar attachment depend on the network artifact ConfigMap name published in CardanoNetwork status rather than falling back to a deterministic name. Added targeted godoc/package overview updates across the new placement/sidecar files and adjusted tests for the stricter artifact-status handoff and metrics selector expectations.

Verification: `moon run root:generate`, `moon run root:test --summary minimal`, `moon run root:check --summary minimal` with the temporary-index workaround, and `git diff --check` passed. A plain `root:check --summary minimal` still fails only at the generated-artifact diff gate because generated API/CRD edits remain intentionally unstaged in the real index.
