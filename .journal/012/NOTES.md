---
id: 012
title: CardanoNetwork artifact ConfigMap
started: 2026-05-23
---

## 2026-05-23 21:06 — Kickoff
Goal for the session: retroactively capture the work to make `CardanoNetwork`
publish exact localnet connection artifacts for the future `CardanoDBSync`
follower-node controller.

Current state of the world: session 011 had just landed the API-only
`CardanoDBSync` CRD in PR #17. The follow-on design question was how db-sync
should find the exact chain files needed to follow the same generated localnet
as the primary node. We rejected putting verbose artifact bytes directly in CR
status and chose a controller-owned ConfigMap whose name is published in
`CardanoNetwork.status.artifacts`.

Work already completed before this journal session was created:
- Split the implementation into two agile slices: first prove a publish-capable
  `cardano-testnet` tools image, then wire the controller/API to consume it.
- Added `yacd-cardano-testnet-publisher` to the tools image, with validation
  that only expected non-secret localnet artifacts are patched into the
  pre-created ConfigMap.
- Updated `yacd-cardano-testnet-init` so localnet init always attempts artifact
  publication after the requested plan is confirmed, including the idempotent
  "environment already exists" path.
- Landed tools-image PR #18 and release-please PR #19. The controller slice now
  uses `ghcr.io/meigma/yacd/cardano-testnet:11.0.1-yacd.2`.
- Added `status.artifacts.networkConfigMapName`, `schemaVersion`, and
  `dataHash` to `CardanoNetwork`, plus an `ArtifactsReady` condition that gates
  aggregate `Ready`.
- Taught the `CardanoNetwork` controller to own `<network>-network-artifacts`,
  `<network>-artifact-publisher`, a resource-name-limited Role, and a
  RoleBinding.
- Kept the primary Deployment on the artifact publisher ServiceAccount with
  pod-level `automountServiceAccountToken: false`; only the localnet init
  container gets the projected API token, CA, and namespace volume.
- Verified the controller only publishes artifact status after the ConfigMap has
  the expected schema annotation, localnet fingerprint annotation, data hash
  annotation, and required keys.
- Added ConfigMap UID pod-template annotation handling so deleting and
  recreating the artifact ConfigMap rolls the primary Pod once and lets the init
  container republish exact generated bytes.
- Extended controller unit tests, manager-backed envtest, workload/init tests,
  and Chainsaw smoke coverage for artifact children, RBAC, projected token
  wiring, readiness/status, foreign-owned conflicts, and deleted ConfigMap
  recovery.

Validation already run:
- `moon run root:generate`
- `moon run root:test`
- `moon run root:check`
- `git diff --check`
- `moon run root:test-e2e`

Important learning: the first Kind smoke caught a real projected-token bug. A
hard-coded token audience caused the publisher patch call to fail with
`401 Unauthorized`; omitting the audience let Kubernetes issue a default API
server token and the full e2e passed.

Current branch and PR state: tools-image PR #18 has merged, the `yacd.2` image
exists, and controller/API PR #20 is open from
`feat/cardanonetwork-artifacts-configmap` with commit `5baef80`
(`feat(cardanonetwork): publish network artifacts configmap`).

Plan: monitor PR #20, handle review or CI feedback, merge through GitHub when
ready, then close this session with the final summary and any durable
`TECH_NOTES.md` updates.
