Preview profile assets in this directory were copied from:

https://book.play.dev.cardano.org/environments/preview/

The planner embeds these files so the controller can render a deterministic
preview public-network workload without reaching the network at reconcile time.
