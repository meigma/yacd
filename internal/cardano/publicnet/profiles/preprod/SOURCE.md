Preprod profile assets in this directory were copied from:

https://book.play.dev.cardano.org/environments/preprod/

The planner embeds these files so the controller can render a deterministic
preprod public-network workload without reaching the network at reconcile time.
